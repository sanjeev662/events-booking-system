VITE_RAZORPAY_KEY=rzp_test_xxxx
# Backend base URL (no trailing slash). Use a safe port (e.g. 5000; avoid 6000, 6666).
# Leave empty to use Vite proxy (/api).
VITE_BACKEND_URL=http://localhost:5000
# Dev server port (only for npm run dev).
VITE_DEV_PORT=6001
